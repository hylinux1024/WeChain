<?xml version="1.0" encoding="utf-8"?>
<org.telegram.ui.ActionBar.DrawerLayoutContainer xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/drawerLayoutContainer"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <org.telegram.ui.ActionBar.ActionBarLayout
            android:id="@+id/actionBarLayout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_above="@+id/tab_layout" />

        <include
            android:id="@+id/tab_layout"
            layout="@layout/tab_layout" />
    </RelativeLayout>

    <org.telegram.ui.Components.PasscodeView
        android:id="@+id/passcodeView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</org.telegram.ui.ActionBar.DrawerLayoutContainer>